const similarAnimals = ["cat", "dog", "tiger"];

similarAnimals.forEach(animal => console.log(animal));
similarAnimals.forEach(animal => console.log(`A ${animal} would be a great pet.`));

console.log("Any of these animals would be a great pet!")